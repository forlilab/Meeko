version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

conda:
  environment: docs/environment.yml

python:
  install:
    - path: .
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
